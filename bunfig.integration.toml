[test]
# Coverage is off for the integration suite, and for the dist-target run that
# shares this config (`bun run test:integration:dist`, BBB_TEST_DIST=1).
#
# There are two separate reasons, and the second one is not the obvious one.
# Both are in AGENTS.md, under Checks — along with the measured behaviour of
# `--config`, which needs the `=`.
coverage = false
